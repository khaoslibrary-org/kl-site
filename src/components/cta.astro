---
import Link from "./ui/link.astro";
import bgImg from 'assets/babel.jpg';
import hexImg from 'assets/hex802.png';
import { Picture } from "@astrojs/image/components";
---

<div
  class="bg-white opacity-70 p-8 md:px-20 md:py-20 mt-20 mx-auto max-w-5xl rounded-lg flex flex-col items-center"
  style={{ backgroundImage: `url('${bgImg.src}')` }}
  >
    <h2 class="text-white opacity-80   text-4xl md:text-6xl tracking-tight text-shadow-md" >
          Universe / Library
    </h2>
    <p class="text-black opacity-30  italic mt-4 text-lg md:text-xl">
    The universe (which others call the Library) is composed of an indefinite, perhaps an infinite, number of hexagonal galleries.
  </p>

  <div class="flex mt-5">
    <Link href="#" class="bg-white/0"  style="inverted">
    <Picture
    src={hexImg}
    alt="hex"
    sizes="(max-width:50px) 100vw, 50px"
    class="invert transition duration-150 ease hover:rotate-[30deg]"
    widths={[50, 50]}
    position="center"
  />
    </Link>
  </div>
</div>

